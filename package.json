{
    "private": true,
    "scripts": {
        "build": "eleventy --input=src",
        "dev": "npm run build -- --serve"
    },
    "dependencies": {
        "@11ty/eleventy": "~0.12",
        "@11ty/eleventy-plugin-syntaxhighlight": "^3",
        "date-fns": "^2"
    }
}
